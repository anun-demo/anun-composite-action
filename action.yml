name: 'ANUN'
description: 'ANUN'
inputs:
  package-url:  # id of input
    description: 'Package url'
    required: true
    default: 'NA'
  installer-url:
    description: 'Install url'
    required: true
    default: 'NA'
  tenant-id:
    description: 'Tenant ID'
    required: true
    default: 'NA'
  secret:
    description: 'Secret'
    required: true
    default: 'NA'
  host:
    description: 'ANUN host'
    required: false
    default: 'api.anun-dev.cloud'
runs:
  using: "composite"
  steps:
    - run: echo INSIDER CONTAINER
      shell: bash
      if: ${{ job.container.id != null }}
    - run: echo NORMAL EXECUTION
      shell: bash
      if: ${{ job.container.id == null }}
   
      
       
